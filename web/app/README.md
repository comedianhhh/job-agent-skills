# app

Next.js front end for the tracker. Setup, endpoints and Docker notes live in [../README.md](../README.md).

```bash
npm install && npm run dev   # expects tracker-api on http://localhost:8000 (NEXT_PUBLIC_API_URL)
```
