# docker compose reads this file. Copy to .env (gitignored) and fill in.
TRACKER_TOKEN=                # required — python -c "import secrets;print(secrets.token_urlsafe(32))"
CAREER_DIR=./career           # your private workspace (tracker.md, targets.yaml)
POSTGRES_PASSWORD=tracker
WEB_PORT=3000
